#DATASETS
library(datasets)

head(mtcars)
summary(mtcars)
plot(mtcars)


#clear packages
detach("package:datasets", unload = TRUE)

#clear plots (if there are any)
dev.off()

#CTRL + L = clear console 
###################################################



